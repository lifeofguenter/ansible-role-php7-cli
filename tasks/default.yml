---

- name: replace default locations with php7
  file:
    src: /opt/php7/bin/{{ item }}
    dest: /usr/bin/php
    owner: root
    group: root
    state: link
  with_items:
    - peardev
    - phpize
    - php-config
    - php
    - phpdbg
    - pear
    - pecl
    - phar
  become: yes
